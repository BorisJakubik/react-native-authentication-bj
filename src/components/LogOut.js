import React from 'react';
import { Button, Card, CardSection } from './common';

export const LogOut = () => (
    <Card>
        <CardSection>
        <Button>
            Logg out
        </Button>
        </CardSection>
    </Card>
);
